# Static hosting for the keeper front (no Node / npm / pnpm / corepack at runtime,
# so the ERR_VM_DYNAMIC_IMPORT_CALLBACK_MISSING corepack crash cannot happen).
#
# "/" serves the keeper landing (index.html). The Sentinel landing stays reachable
# at /sentinel (or /sentinel.html).

DirectoryIndex index.html

RewriteEngine On

# Extension-less path -> .html when that file exists (e.g. /sentinel)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^(.+?)/?$ $1.html [L]
